package mqq.observer;

import android.os.Bundle;

public abstract class CheckConErroObserver implements BusinessObserver {
    public void onReceive(int type, boolean isSuccess, Bundle bundle) {
    }
}
